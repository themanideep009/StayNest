apiVersion: v1
kind: ConfigMap
metadata:
  name: staynest-config
  namespace: staynest
data:
  NODE_ENV: "production"
  PORT: "3000"
  APP_BASE_URL: "https://staynest.example.com"
  JWT_EXPIRES_IN: "7d"
